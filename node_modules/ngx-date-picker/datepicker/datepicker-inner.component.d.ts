import { EventEmitter, OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { DateFormatter } from './date-formatter';
import { DropdownState } from './dropdown.state';
export declare class DatePickerInnerComponent implements OnInit, OnChanges {
    private _state;
    datepickerMode: string;
    type: string;
    startingDay: number;
    yearRange: number;
    minDate: Date;
    maxDate: Date;
    minMode: string;
    maxMode: string;
    showWeeks: boolean;
    formatDay: string;
    formatMonth: string;
    formatYear: string;
    formatDayHeader: string;
    formatDayTitle: string;
    formatMonthTitle: string;
    onlyCurrentMonth: boolean;
    shortcutPropagation: boolean;
    customClass: {
        date: Date;
        mode: string;
        clazz: string;
    }[];
    monthColLimit: number;
    yearColLimit: number;
    dateDisabled: {
        date: Date;
        mode: string;
    }[];
    initDate: Date;
    selectionDone: EventEmitter<{
        date: Date;
        timezone: number;
    }>;
    update: EventEmitter<{
        date: Date;
        timezone: number;
    }>;
    activeDateChange: EventEmitter<Date>;
    stepDay: any;
    stepMonth: any;
    stepYear: any;
    protected modes: string[];
    protected dateFormatter: DateFormatter;
    protected uniqueId: string;
    protected _activeDate: Date;
    protected _timezone: number;
    protected selectedDate: Date;
    protected activeDateId: string;
    protected refreshViewHandlerDay: Function;
    protected compareHandlerDay: Function;
    protected refreshViewHandlerMonth: Function;
    protected compareHandlerMonth: Function;
    protected refreshViewHandlerYear: Function;
    protected compareHandlerYear: Function;
    protected refreshViewHandlerHour: Function;
    protected compareHandlerHour: Function;
    protected refreshViewHandlerMinute: Function;
    protected compareHandlerMinute: Function;
    protected refreshViewHandlerTimezone: Function;
    activeDate: Date;
    timezone: number;
    constructor(_state: DropdownState);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    setCompareHandler(handler: Function, type: string): void;
    compare(date1: Date, date2: Date, time?: string): number | undefined;
    setRefreshViewHandler(handler: Function, type: string): void;
    refreshView(): void;
    dateFilter(date: Date, format: string): string;
    isActive(dateObject: any, time?: string): boolean;
    createDateObject(date: Date, format: string, time?: string): any;
    split(arr: any[], size: number): any[];
    fixTimeZone(date: Date): Date;
    select(date: Date, isManual?: boolean): void;
    move(event: any, direction: number): void;
    toggleMode(event: any, direction: number): void;
    protected getCustomClassForDate(date: Date): string;
    protected compareDateDisabled(date1Disabled: {
        date: Date;
        mode: string;
    }, date2: Date): number;
    protected isDisabled(date: Date): boolean;
}
