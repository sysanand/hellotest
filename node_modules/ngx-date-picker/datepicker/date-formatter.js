import moment from 'moment';
export var DateFormatter = (function () {
    function DateFormatter() {
    }
    DateFormatter.prototype.format = function (date, format) {
        return moment(date.getTime()).format(format);
    };
    DateFormatter.prototype.toDate = function (date) {
        var currentDate = moment.parseZone(date);
        return moment.isMoment(currentDate)
            ? new Date(parseInt(currentDate.format('YYYY')), parseInt(currentDate.format('MM')) - 1, parseInt(currentDate.format('DD')), parseInt(currentDate.format('H')), parseInt(currentDate.format('m')))
            : void 0;
    };
    return DateFormatter;
}());
//# sourceMappingURL=date-formatter.js.map