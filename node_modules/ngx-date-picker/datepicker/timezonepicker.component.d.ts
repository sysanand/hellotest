import { OnInit } from '@angular/core';
import { DatePickerInnerComponent } from './datepicker-inner.component';
import { DatepickerConfig } from './datepicker.config';
export declare class TimezonePickerComponent implements OnInit {
    datePicker: DatePickerInnerComponent;
    displayTimeZones: Object[];
    minPos: number;
    maxPos: number;
    timeZones: Object[];
    constructor(datePicker: DatePickerInnerComponent, config: DatepickerConfig);
    ngOnInit(): void;
    move(event: any, direction: string): void;
    setTimezone(event: any, zone: number): void;
    createTimezoneObject(zone: any): any;
    isActive(zone: number): boolean;
    private clone(value);
}
